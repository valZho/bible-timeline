'use client';

import React from 'react';
// import type { AppProps } from 'next/app';
import PageWrapper from '@/components/Page';

function Page() {
  return <PageWrapper />;
}

export default Page;
